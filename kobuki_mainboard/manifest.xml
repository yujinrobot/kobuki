<package>
  <description brief="iclebo_mainboard">

     iclebo_mainboard

  </description>
  <author>jakan2</author>
  <license>Yujin</license>
  <review status="unreviewed" notes=""/>
  <url>http://yujin.dnsdojo.com:3000/wiki/ycs/</url>
  
  <!-- ecl -->
  <depend package="ecl_containers"/>
  <depend package="ecl_sigslots"/>
  <depend package="ecl_devices"/>
  <depend package="ecl_signals"/>
  <depend package="ecl_threads"/>
  <depend package="ecl_time"/>  
  
  <!-- ycs -->
  <depend package="packet_handler"/>
  
  <export>
    <cpp cflags="-I${prefix}/include"/>
  </export>  

<!--
  Typical options for a ycs-ros cpp style package providing headers/libs in the example below
  <export>
    <cpp cflags="-I${prefix}/include/share -I${prefix}/msg/cpp -I${prefix}/srv/cpp" lflags="-Wl,-rpath,${prefix}/lib -L${prefix}/lib -lgoo"/>
  </export>
  
  Really common dependencies for a typical c++ ycs-ros control node:
  <depend package="roscpp"/>        // c++ ros nodes
  
  Including a system package as a dependency
  <rosdep name="boost"/>
-->
</package>
